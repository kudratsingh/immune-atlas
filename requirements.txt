# Runtime dependencies. Ranges rather than exact pins so `make setup` resolves on
# the Codespaces default image; CI runs the same resolution on Python 3.12.
pandas>=2.2,<4
numpy>=1.26,<3
scipy>=1.11,<2
matplotlib>=3.8,<4
jsonschema>=4.20,<5
